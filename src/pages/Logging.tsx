import { LoggingCard } from "@/components";

export default function Logging() {
  return (
    <section className="flex h-[calc(100vh-3.5rem)] justify-center items-center">
      <LoggingCard />
    </section>
  );
}
